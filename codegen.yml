overwrite: true
schema: 'http://localhost:4000/graphql'
documents: './src/graphql/schema.sql'
generates:
  src/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
  ./graphql.schema.json:
    plugins:
      - 'introspection'
