# Bookeezy-api

GraphQL backend for Bookeezy API

## Deno

Run without Docker

```bash
deno run --allow-net server.ts
```

## Docker

Run with Docker

```bash
docker build -t bookeezy-api .
```

```bash
docker-compose up -d
```
